{
  "name": "unix-users",
  "version": "0.0.1",
  "description": "A library for listing and authenticating users in Ubuntu Server",
  "author": "Jim",
  "dependencies": {
    "bluebird": "^1.0.7",
    "lodash": "^2.4.1"
  },
  "devDependencies": {
    "mocha": "^1.17.1",
    "LiveScript": "^1.2.0",
    "should": "^3.1.3"
  },
  "scripts": {
    "test": "mocha --compilers ls:LiveScript -r should -R spec"
  },
  "main": "./src/main.ls"
}
